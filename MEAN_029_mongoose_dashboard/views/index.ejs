<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk"
        crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>MEAN 029: Mongoose Dashboard</title>
</head>

<body>
    <div class="container">
        <div class="jumbotron">
            <h1>PACK</h1>
            <% if(messages.errors){ %>
            <ul>
                <% for(var err of messages.errors){ %>
                <li><%= err %></li>
                <% } %>
            </ul>
            <% } %>
            <a class="btn btn-success" href="/dogs/new/">New</a>
        </div>
        <table class="table table-striped">
            <thead class="thead-dark">
                <tr>
                    <th>Name</th>
                    <th>Breed</th>
                    <th>Color</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <% for(var d of dogs){ %>
                <tr>
                    <td><a href="/<%= d._id %>"><%= d.name %></a></td>
                    <td><%= d.breed %></td>
                    <td><%= d.color %></td>
                    <td>
                        <div class="row">
                            <div class="col">
                                <form class="form-inline" action="/dogs/destroy/<%= d._id %>" method="post">
                                    <div class="form-group">
                                        <a class="btn btn-link" href="/dogs/edit/<%= d._id %>">Edit </a>
                                    </div>
                                    |
                                    <div class="form-group">
                                        <input class="btn btn-link" type="submit" value="Delete">
                                    </div>
                                </form>
                            </div>
                        </div>
                    </td>
                </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</body>

</html>